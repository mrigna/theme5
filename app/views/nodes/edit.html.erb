<%= form_for @node, url: {action: "update"} do |node| %>
<%= node.collection_select(:group, Node.user(@user).where.not(:group => nil), :group, :group, {:selected => @node.group}, :include_blank => true) %>
<%= submit_tag("update") %>
<% end %>